# Misty bundled config (resources).
#
# This file is shipped alongside the app's read-only assets in releases.
# User-editable runtime configuration lives in:
#   ~/misty/config/misty.json
#
# Kept intentionally minimal to avoid duplicated sources of truth.

