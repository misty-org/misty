import type { HTMLAttributes, ReactNode } from "react";
import {
  BookOpenText,
  Bot,
  CheckSquare2,
  MessagesSquare,
  Settings2,
  Users,
  type LucideIcon,
} from "lucide-react";
import { cn } from "@/ui";

export type SpacePageSection = "chat" | "agents" | "tasks" | "library" | "members" | "settings";
