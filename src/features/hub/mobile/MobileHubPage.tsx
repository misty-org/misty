import { mobilePageClass } from "../../../app/mobileStyles";

export function MobileHubPage() {
  return (
    <section className={`${mobilePageClass} grid place-items-center`}>
      <div className="text-xl font-bold text-[var(--misty-text)]">Coming soon...</div>
    </section>
  );
}
