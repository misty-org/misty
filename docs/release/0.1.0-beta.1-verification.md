# 0.1.0-beta.1 preparation evidence

This records preparation checks on September 6, 2026. It is not approval to publish the beta. The installation and update gates in `release/validation.json` remain unset.

## Completed checks

- Public SDK/contracts 0.1.0: 54 tests and isolated packed-package consumption passed. The SDK source is pinned at `ad375b78cc6619f4667f5ba19e273547958b08fb`; its GitHub draft contains both verified archives and `SDK-SHA256SUMS`. npm publication is deferred.
- All ten desktop components built successfully with package boundary enforcement, SHA-256 checksums, and the separate release signature. The [package job](https://github.com/misty-org/misty/actions/runs/34020212125) uploaded `beta-packages` for host revision `11827479f3d51469885a839b29caba2500706a5f`. This workflow artifact is not a completed installer release.
- Desktop type checking and a frontend build from clean staged source passed. Focused Code/editor/project tests, app compatibility/update reservation tests, and Browser/Files/Discover regression tests passed.
- Six native package verification and interrupted-install recovery tests passed. The two real Restic repository/restore checks passed locally after installing Restic; CI now installs that prerequisite explicitly.
- A real local clangd process passed initialization, hover, definitions, symbols, diagnostics, references, rename, and cleanup through the SDK and host project mapping. Reproduce with `node scripts/sdk-code-lsp-probe.mjs /tmp/misty-code-lsp-check --project-rpc`. This probe substitutes the native device transport; it does not establish end-to-end installed Code or directory-grant behavior.
- The Apps repository passed its 30 tests, catalog validation, and site build. Full Go tests passed in the backend CI run at `87d51d3e01e338213089a55e0f78bbde6f926b00`; subsequent fixture corrections are being verified on the backend PR.
- Source scanning found no real credentials in the prepared source. Required vendored dependencies are retained; release private keys remain outside the repositories.

## Blocked or not yet exercised

- Apple Silicon compilation completed in [the release run](https://github.com/misty-org/misty/actions/runs/34020212125), but notarization returned HTTP 401, invalid Apple ID/app-specific password. Certificate import succeeded. No notarized installer or verified updater archive was produced. Refresh the GitHub notarization secret and rerun preparation.
- Intel compilation and signing must be checked independently in the release workflow. No real Intel installer installation has been performed.
- No real final-installer installation has been performed on Apple Silicon either. The local Mac was locked when native UI access was attempted. The native Browser probe timed out; its dropdown/WebKit regression is not marked verified by that attempt.
- A desktop update between two beta versions with user data preserved has not been run. Neither have the complete installed Code flows nor the all-ten-app interactive smoke check. Automated component and recovery checks do not replace these gates.

No host beta assets, live app catalog, update feed, npm package, or Go deployment has been promoted. Main branches remain unmerged. Follow [the macOS beta procedure](macos-beta.md) to finish signing, installation checks, and explicit promotion.
