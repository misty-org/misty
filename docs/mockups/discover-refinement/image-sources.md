# Discover mockup image sources

- `library.webp`: copied unchanged from `misty-website/public/screenshots/misty-space-library.webp`, an existing product screenshot in the website repository. It depicts an earlier Library interface.
- `journal.png`: copied unchanged from `/tmp/misty-sdk-journal-render/canvas.png`, produced by the actual compiled Journal component rendering fixture. The drawing is test content, not user data.

CSS frames the original captures; no controls or text have been painted into either image. This mockup illustrates the use of real product imagery. Final catalog imagery should be newly captured with useful, curated sample projects in the current app versions.
