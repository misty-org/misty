# Do Not Upload

These PNGs are a ButterKit draft export and include a visible `Made with ButterKit` watermark.

They were generated before final UI approval, so they are not App Review-ready even aside from the watermark.

They are retained only as proof that ButterKit MCP became reachable and exported artboards. Do not resume ButterKit/App Store screenshot production until the mobile UI is approved.
