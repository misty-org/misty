import "@/styles/styles.css";
import { createSdkFilesComponent } from "./workspace/createSdkFilesComponent";

// The downloaded package owns its workspace, previews, state, and transfer UI.
export default createSdkFilesComponent();
